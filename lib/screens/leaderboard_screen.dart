import 'package:flutter/material.dart';


class LeaderBoardScreenWidget extends StatelessWidget {
 final Color color;

 LeaderBoardScreenWidget(this.color);

 @override
 Widget build(BuildContext context) {
   return Container(
     color: color,
   );
 }
}